#include <bits/stdc++.h>

using namespace std;

#define ll long long

ll n, k;

int main() {
    scanf("%lld %lld", &n, &k);

    vector < int > ans;
    set < int > lft;
    for (int i = 1; i <= n; i++)
        lft.insert(i);

    for (int i = 0; i < n; i++) {
        for (int v : lft) {
            if (k > )

        }

    }
    return 0;
}
