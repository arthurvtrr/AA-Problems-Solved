#include <bits/stdc++.h>

using namespace std;

int n, nums[100100];

int main() {
    //freopen("hell.in", "r", stdin);
    //freopen("hell.out", "w", stdout);

    scanf("%d", &n);
    for (int i = 0; i < n; i++)
        scanf("%d", &nums[i]);


    return 0;
}
